{
  "name": "spryng",
  "version": "1.0.0",
  "description": "NodeJS SDK for the Spryng SMS API",
  "main": "dist/index.js",
  "repository": "https://github.com/spryng/rest-api-node",
  "author": "Roemer Bakker",
  "license": "MIT",
  "private": false,
  "dependencies": {
    "axios": "^0.20.0",
    "qs": "^6.9.4"
  },
  "scripts": {
    "build": "rm -rf ./dist && tsc -p tsconfig.json",
    "test": "yarn build && node test.js",
    "prepublish": "tsc -p tsconfig.json"
  },
  "types": "dist/index.d.ts",
  "devDependencies": {
    "@types/node": "^14.11.2",
    "@types/qs": "^6.9.5",
    "typescript": "^4.0.3"
  }
}
